add_opp_run(simple-security CONFIG omnetpp.ini)
add_opp_test(simple-security SUFFIX omnidirectional CONFIG omnidirectional_test)
add_opp_test(simple-security SUFFIX directional CONFIG directional_test)